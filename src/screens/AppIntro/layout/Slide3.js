import React from 'react';
import {Text, View} from "react-native";

const Slide3 = (props) => {
    return (
        <View style={props.slideStyle}>
            <Text style={props.textStyle}>And simple</Text>
        </View>
    )
};
export default Slide3;